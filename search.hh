#ifndef SEARCH_HH
#define SEARCH_HH

/// \file
/// Main loop (search()), to search for good options.

/// Main search function, to find "best" compiler options.
void search();

/// Print out the compiler version.
void summary_first();

/// Print out statistics and end result.
void summary_exit();

#endif
